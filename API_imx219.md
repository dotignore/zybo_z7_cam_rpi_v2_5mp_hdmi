# API IMX219

Описание функций камеры Sony IMX219PQH5-C на плате Zybo Z7 (разъём Pcam, вывод HDMI).

Это контракт API, а не реализация. Регистры и пределы взяты из `doc_imx219/pdf/IMX219_datasheet/IMX219_datasheet.md`. Текущая прошивка держит один рабочий режим: **1920×1080, RAW10, Bayer GBRG, 2 линии CSI-2, 30 кадр/с**.

Чип: активная матрица **3280×2464**, пиксель 1.12 µm, диагональ 4.60 mm (Type 1/4). I2C-адрес на этой плате **0x10** (запасной 0x1A). Идентификатор `0x0000/0x0001` = **0x0219**.

В документе **46 функций**. Раздел 10 — это шаги одного запуска, а не полный список. Те шаги только выставляют режим и берут кадр. Рядом с каждой установкой есть чтение, плюс функции, которые в этот порядок не входят.

После `imx219_open` уже стоят 1920×1080, RAW10, 30 кадр/с, GBRG, 2 линии, автояркость 80 и таблица `IMX219_CC_IDENTITY`. Шаги 2–8 в разделе 10 нужны только когда эти значения меняют.

**Основной контракт, 31 функция**

| Группа | Функции |
| --- | --- |
| Жизненный цикл | `imx219_open`, `imx219_close`, `imx219_start`, `imx219_stop`, `imx219_is_streaming`, `imx219_configure`, `imx219_get_config` |
| Режим | `imx219_set_video_mode`, `imx219_get_video_mode`, `imx219_set_pixel_format`, `imx219_get_pixel_format`, `imx219_check_link` |
| Кадр | `imx219_grab`, `imx219_frame_stride`, `imx219_frame_bytes` |
| Демозаика | `imx219_set_demosaic`, `imx219_get_demosaic` |
| Линии | `imx219_set_lanes`, `imx219_get_lanes` |
| Яркость | `imx219_set_brightness_mode`, `imx219_get_brightness_mode`, `imx219_set_brightness`, `imx219_get_brightness`, `imx219_get_brightness_status` |
| Цвет | `imx219_set_color_table`, `imx219_get_color_table`, `imx219_get_color_table_info`, `imx219_get_color_status` |
| Надпись на экране | `imx219_set_osd`, `imx219_get_osd`, `imx219_get_stream_stats` |

**Нижний слой, раздел 8, ещё 15 функций**

`imx219_set_exposure_lines`, `imx219_get_exposure_lines`, `imx219_set_exposure_us`, `imx219_set_analog_gain`, `imx219_set_digital_gain_q88`, `imx219_set_orientation`, `imx219_set_crop`, `imx219_set_binning`, `imx219_set_test_pattern`, `imx219_read_temperature_c`, `imx219_select_frame_bank`, `imx219_set_embedded_data`, `imx219_get_levels`, `imx219_read_chip_id`, `imx219_reset`.

---

## 1. Что API принимает и чего не меняет

| Параметр | Поведение |
| --- | --- |
| Разрешение экрана | Любое из списка ниже, высота от 1080 строк и меньше |
| Частота кадров | 24, 30 и остальные стандартные частоты этого разрешения |
| Кодирование цвета | `RAW10`, `RAW8`, `COMP8` |
| Число линий CSI-2 | Всегда **2**. Параметр есть в конфигурации, вызов его не меняет |
| 4 линии | Сенсор их умеет, на этой модели недоступны |
| Демозаика | Все 12 вариантов мозаики; с сенсора приходят только 4 классических Bayer |
| Яркость | Ручная шкала 0…255 или автомат с той же шкалой как целью |
| Калибровка цвета | Выбор таблицы под источник света этой камеры |
| Надпись на HDMI | Кадры в секунду на экране и число отброшенных кадров за последние 5 минут |

Один вызов задаёт режим целиком. Пока идёт поток, смена разрешения, формата или мозаики останавливает поток, применяет режим и запускает его снова. Яркость, выдержка и усиление применяются со следующего кадра без остановки. Смена таблицы цвета тоже без остановки: новые коэффициенты действуют на следующий кадр.

---

## 2. Линии CSI-2

```
imx219_lane_count_t imx219_get_lanes(const imx219_t *cam);
```

Возвращает всегда `IMX219_LANES_2`.

```
imx219_status_t imx219_set_lanes(imx219_t *cam, imx219_lane_count_t lanes);
```

Параметр **не меняется**. `IMX219_LANES_2` возвращает `IMX219_OK` и оставляет 2 линии. Любое другое значение, включая `IMX219_LANES_4`, возвращает `IMX219_ERR_LANES_FIXED` и ничего не пишет в сенсор.

Почему 4 линии закрыты:

- Кристалл IMX219 выбирает 2 или 4 линии (`0x0114`: `0x01` = 2 линии, `0x03` = 4 линии). Заводское значение регистра — 4 линии.
- На этой модели разведены только две пары данных и тактовая. Потолок линии — **912 Мбит/с**, суммарно **1.824 Гбит/с** (пример тактирования даташита для 2 линий, полный кадр не быстрее 21 кадр/с).
- Четыре линии у кристалла дают до 755 Мбит/с на линию и полный кадр 3280×2464 @ 30 кадр/с, а также пример 180 кадр/с при аналоговом биннинге. Этой пропускной способности на плате нет, поэтому API 4 линии не включает.

Перед выходом из standby в сенсор один раз записывается 2 линии. Повторные вызовы `imx219_set_lanes` эту запись не трогают.

---

## 3. Разрешения экрана

Высота кадра — от **1080** и ниже. Шире активной матрицы 3280×2464 режим не бывает. 1080p на сенсоре — это вырезка по вертикали без биннинга, не уменьшение всей матрицы.

Имена ниже — стандартные режимы экрана (CEA/CTA-861, телевидение, VESA). Каждое имя — отдельное значение `imx219_mode_id_t`.

| ID | Разрешение | Имя | Соотношение |
| --- | --- | --- | --- |
| `IMX219_MODE_1080P` | 1920×1080 | Full HD, 1080p | 16:9 |
| `IMX219_MODE_1600x900` | 1600×900 | HD+ | 16:9 |
| `IMX219_MODE_1440x900` | 1440×900 | WXGA+ | 16:10 |
| `IMX219_MODE_1366x768` | 1366×768 | WXGA | ~16:9 |
| `IMX219_MODE_1360x768` | 1360×768 | WXGA | ~16:9 |
| `IMX219_MODE_1280x1024` | 1280×1024 | SXGA | 5:4 |
| `IMX219_MODE_1280x960` | 1280×960 | SXGA− | 4:3 |
| `IMX219_MODE_1280x800` | 1280×800 | WXGA | 16:10 |
| `IMX219_MODE_1280x768` | 1280×768 | WXGA | 5:3 |
| `IMX219_MODE_720P` | 1280×720 | HD, 720p | 16:9 |
| `IMX219_MODE_1152x864` | 1152×864 | XGA+ | 4:3 |
| `IMX219_MODE_1024x768` | 1024×768 | XGA | 4:3 |
| `IMX219_MODE_1024x600` | 1024×600 | WSVGA | ~17:10 |
| `IMX219_MODE_960x540` | 960×540 | qHD | 16:9 |
| `IMX219_MODE_854x480` | 854×480 | FWVGA | ~16:9 |
| `IMX219_MODE_800x600` | 800×600 | SVGA | 4:3 |
| `IMX219_MODE_800x480` | 800×480 | WVGA | 5:3 |
| `IMX219_MODE_576P` | 720×576 | 576p | 5:4 (PAL) |
| `IMX219_MODE_480P` | 720×480 | 480p | 3:2 (NTSC) |
| `IMX219_MODE_VGA` | 640×480 | VGA | 4:3 |
| `IMX219_MODE_640x360` | 640×360 | nHD | 16:9 |
| `IMX219_MODE_480x360` | 480×360 | — | 4:3 |
| `IMX219_MODE_426x240` | 426×240 | NTSC wide, 240p | ~16:9 |
| `IMX219_MODE_QVGA` | 320×240 | QVGA | 4:3 |

Режим по умолчанию после `imx219_open`: `IMX219_MODE_1080P`.

Чересстрочные режимы (1080i, 576i, 480i) в API нет: сенсор и HDMI-тракт отдают прогрессивный кадр.

---

## 4. Частоты кадров

Для **каждого** разрешения из таблицы выше API принимает общий набор и, где он стандартный для этого формата, дополнительные значения.

Общий набор, есть у всех разрешений раздела 3:

| ID | Частота | Где это стандарт |
| --- | --- | --- |
| `IMX219_FPS_24` | 24 | кино |
| `IMX219_FPS_25` | 25 | PAL / 50 Гц |
| `IMX219_FPS_30` | 30 | NTSC-целое / компьютерное 30p |
| `IMX219_FPS_50` | 50 | PAL / 720p50 / 1080p50 |
| `IMX219_FPS_60` | 60 | NTSC-целое / VESA 60 Гц |

Дополнительно, только у тех режимов, где такая частота входит в стандарт формата:

| ID | Частота | Для каких разрешений |
| --- | --- | --- |
| `IMX219_FPS_23_976` | 23.976 (24/1.001) | 1920×1080, 1280×720 |
| `IMX219_FPS_29_97` | 29.97 (30/1.001) | 1920×1080, 1280×720 |
| `IMX219_FPS_59_94` | 59.94 (60/1.001) | 1920×1080, 1280×720, 720×480, 640×480 |
| `IMX219_FPS_56` | 56 | 800×600 (VESA) |
| `IMX219_FPS_70` | 70 | 1024×768 (VESA) |
| `IMX219_FPS_72` | 72 | 800×600, 640×480 (VESA) |
| `IMX219_FPS_75` | 75 | 1280×1024, 1024×768, 800×600, 640×480 (VESA) |

Частота по умолчанию: `IMX219_FPS_30`.

Сводка «разрешение → какие частоты стандартны и принимаются»:

| Разрешение | Стандартные частоты |
| --- | --- |
| 1920×1080 | 23.976, 24, 25, 29.97, 30, 50, 59.94, 60 |
| 1280×720 | 23.976, 24, 25, 29.97, 30, 50, 59.94, 60 |
| 720×576 | 24, 25, 30, 50, 60 |
| 720×480 | 24, 25, 29.97, 30, 50, 59.94, 60 |
| 640×480 | 24, 25, 29.97, 30, 50, 56, 59.94, 60, 72, 75 |
| 800×600 | 24, 25, 30, 50, 56, 60, 72, 75 |
| 1024×768 | 24, 25, 30, 50, 60, 70, 75 |
| 1280×1024 | 24, 25, 30, 50, 60, 75 |
| 1600×900, 1440×900, 1366×768, 1360×768, 1280×960, 1280×800, 1280×768, 1152×864, 1024×600, 960×540, 854×480, 800×480, 640×360, 480×360, 426×240, 320×240 | 24, 25, 30, 50, 60 |

Предел сенсора и двух линий важнее списка. Вызов с парой, которая не влезает в 912 Мбит/с × 2, возвращает `IMX219_ERR_BANDWIDTH`.

Ориентиры даташита, уже с учётом того, что 4 линий нет:

- 1920×1080, вырезка по вертикали, без биннинга: до **60** кадр/с. 100 и 120 кадр/с у 1080p в CTA бывают, здесь их нет.
- 3280×2464 на 2 линиях, RAW10: до **21** кадр/с. 30 кадр/с полного кадра в даташите сняты на 4 линиях и в эту модель не входят.
- 180 кадр/с около 720p с аналоговым биннингом 2×2 в даташите есть, пример тактирования — 4 линии, выход 1408×792. На 2 линиях этот поток шире 1.824 Гбит/с, API его не отдаёт.
- Оценка полезной нагрузки: `ширина × высота × кадр/с × бит_на_пиксель`. RAW10 — 10 бит, RAW8 и COMP8 — 8 бит. Запас на заголовки CSI-2, CRC и гашение должен оставаться внутри 1.824 Гбит/с. 1920×1080×60×10 бит = 1.244 Гбит/с и проходит. Полный кадр 3280×2464×30×10 бит = 2.42 Гбит/с и не проходит.

```
imx219_status_t imx219_set_video_mode(imx219_t *cam,
                                      imx219_mode_id_t mode,
                                      imx219_fps_t fps);
imx219_status_t imx219_get_video_mode(const imx219_t *cam,
                                      imx219_mode_id_t *mode,
                                      imx219_fps_t *fps,
                                      uint16_t *width,
                                      uint16_t *height);
```

`imx219_set_video_mode` проверяет, что частота входит в стандартный набор этого разрешения и что поток выбранного цветового формата влезает в 2 линии. Иначе режим не меняется.

---

## 5. Кодирование цвета

```
imx219_status_t imx219_set_pixel_format(imx219_t *cam, imx219_pixfmt_t fmt);
imx219_pixfmt_t imx219_get_pixel_format(const imx219_t *cam);
```

| ID | Имя | На проводе CSI-2 | Уровень чёрного | Примечание |
| --- | --- | --- | --- | --- |
| `IMX219_PIX_RAW10` | RAW10 | Data Type `0x2B`, формат `0x0A0A` | 64 | Значение по умолчанию. 10 бит АЦП без сжатия |
| `IMX219_PIX_RAW8` | RAW8 | Data Type `0x2A`, формат `0x0808` | 16 | Те же 10 бит, отданные как 8: чёрный уровень даташита делится на 4 |
| `IMX219_PIX_COMP8` | COMP8 | 8 бит после сжатия 10→8 | 64 | Сжатие включается отдельно от RAW8. Чёрный остаётся 64, как у RAW10 |

АЦП сенсора всегда 10 бит. RAW8 — это 8-битный выход. COMP8 — включённое сжатие 10 в 8 (`compression capability` у кристалла есть). Смешивать RAW8 и COMP8 нельзя: это разные режимы.

Смена формата меняет число бит на пиксель, поэтому её нужно делать вместе с проверкой полосы (`imx219_set_video_mode` после формата, либо один общий `imx219_configure`). Пока формат не применён, поток не стартует.

Порядок байт после демозаики в этом проекте: в DDR лежит **G, B, R** (GBR888), на HDMI 24 бита `{R, B, G}`.

---

## 6. Демозаика

Физический фильтр IMX219 — обычный Байер. При `image_orientation = 0` первая строка **Gb B**, вторая **R Gr**. Это фаза **GBRG**.

Зеркало и переворот (даташит, readout) меняют фазу на проводе. Четыре положения ориентации дают четыре классические фазы:

| Ориентация | Зеркало H | Переворот V | Фаза на выходе |
| --- | --- | --- | --- |
| `0` | нет | нет | GBRG |
| `1` | да | нет | BGGR |
| `2` | нет | да | RGGB |
| `3` | да | да | GRBG |

```
imx219_status_t imx219_set_demosaic(imx219_t *cam, imx219_mosaic_t mosaic);
imx219_mosaic_t imx219_get_demosaic(const imx219_t *cam);
```

Все варианты, которые API называет. Клетка 2×2 — левый верхний пиксель кадра первым.

| ID | Значение | Клетка 2×2 | Откуда берётся |
| --- | --- | --- | --- |
| `IMX219_MOSAIC_RGGB` | 0 | R G / G B | ориентация 2 |
| `IMX219_MOSAIC_GRBG` | 1 | G R / B G | ориентация 3 |
| `IMX219_MOSAIC_GBRG` | 2 | G B / R G | ориентация 0, фаза даташита |
| `IMX219_MOSAIC_BGGR` | 3 | B G / G R | ориентация 1 |
| `IMX219_MOSAIC_RGBG` | 4 | R G / B G | только имя в API |
| `IMX219_MOSAIC_RBGG` | 5 | R B / G G | только имя в API |
| `IMX219_MOSAIC_GRGB` | 6 | G R / G B | только имя в API |
| `IMX219_MOSAIC_GGRB` | 7 | G G / R B | только имя в API |
| `IMX219_MOSAIC_GGBR` | 8 | G G / B R | только имя в API |
| `IMX219_MOSAIC_GBGR` | 9 | G B / G R | только имя в API |
| `IMX219_MOSAIC_BRGG` | 10 | B R / G G | только имя в API |
| `IMX219_MOSAIC_BGRG` | 11 | B G / R G | только имя в API |

Фазы 0…3 совпадают с сенсором и с двухбитным выбором фазы в текущей плате. Фазы 4…11 API перечисляет полностью: это остальные раскладки 2×2 из R, G, B. Цветной фильтр IMX219 их не формирует. `imx219_set_demosaic` для 4…11 возвращает `IMX219_ERR_MOSAIC`, пока в тракте нет отдельного демозаика под такую клетку.

Значение по умолчанию в описании сенсора — `IMX219_MOSAIC_GBRG`. Смена фазы не перекрашивает уже записанный в DDR кадр: действует со следующего кадра после демозаика.

---

## 7. Функции

### 7.1 Жизненный цикл

```
imx219_status_t imx219_open(imx219_t *cam);
void            imx219_close(imx219_t *cam);
```

`imx219_open` ждёт питание, поднимает XCLR из 0 в 1 и выдерживает не меньше **6 мс** до выхода из standby. Читает идентификатор `0x0219`. Если чип не ответил — `IMX219_ERR_PROBE`. После успеха камера в standby, режим 1920×1080, RAW10, 30 кадр/с, 2 линии, мозаика GBRG, поток выключен.

`imx219_close` останавливает поток (`0x0100 = 0`) и отпускает XCLR.

```
imx219_status_t imx219_start(imx219_t *cam);
imx219_status_t imx219_stop(imx219_t *cam);
bool            imx219_is_streaming(const imx219_t *cam);
```

`imx219_start` включает поток (`0x0100 = 1`). `imx219_stop` возвращает software standby (`0x0100 = 0`). Повторный start без stop — `IMX219_ERR_STATE`.

```
imx219_status_t imx219_configure(imx219_t *cam, const imx219_config_t *cfg);
imx219_status_t imx219_get_config(const imx219_t *cam, imx219_config_t *cfg);
```

`imx219_config_t` собирает режим экрана, частоту, формат пикселя, мозаику и число линий. Поле линий при чтении всегда 2. При записи значение, отличное от 2, даёт `IMX219_ERR_LANES_FIXED`, остальные поля не применяются.

### 7.2 Запрос полосы

```
imx219_status_t imx219_check_link(imx219_mode_id_t mode,
                                  imx219_fps_t fps,
                                  imx219_pixfmt_t fmt);
```

Считает полезную нагрузку и отвечает `IMX219_OK` или `IMX219_ERR_BANDWIDTH`. Сенсор не трогает. Нужен до `imx219_configure`, чтобы не гасить картинку отказом.

### 7.3 Кадр

```
imx219_status_t imx219_grab(imx219_t *cam,
                            void *dst,
                            size_t dst_bytes,
                            imx219_frame_info_t *info,
                            uint32_t timeout_ms);
```

Кладёт в `dst` один целый кадр после демозаики (GBR888) либо сырой кадр, если демозаика в конфигурации обходится отдельным флагом захвата. `info` получает ширину, высоту, stride, формат, фазу, номер кадра и метку времени. Короткий буфер — `IMX219_ERR_NOSPC`. Таймаут — `IMX219_ERR_TIMEOUT`.

```
uint32_t imx219_frame_stride(imx219_mode_id_t mode, imx219_pixfmt_t fmt);
size_t   imx219_frame_bytes(imx219_mode_id_t mode, imx219_pixfmt_t fmt);
```

Для GBR888 stride = ширина × 3. Для сырого RAW10 упаковка CSI — 4 пикселя на 5 байт.

### 7.4 Яркость

На кристалле нет автоэкспозиции. Яркость собирается снаружи из трёх регистров сенсора, в одном и том же порядке: выдержка в строках, затем аналоговое усиление, затем цифровое. Пока хватает выдержки, усиление остаётся 1×.

| Ступень | Диапазон | Смысл |
| --- | --- | --- |
| Выдержка | 8 … `frame_length_lines − 4` строк | Для текущего 1080p это 8…1516. Даташит: максимум обычного кадра = длина кадра минус 4 |
| Аналоговое усиление | код 0…232 | Кратность `256 / (256 − код)`. 0 = 1×, 232 ≈ 10.7× |
| Цифровое усиление | `0x0100` … `0x0400` | Формат 8.8. `0x0100` = 1×, `0x0400` = 4× |

Даташит допускает цифровое усиление выше 4×. Шкала яркости этого API на 4× останавливается: дальше картинка на HDMI пересвечивается без новой информации с матрицы.

Шкала одна и та же в обоих режимах: **0…255**, яркость Rec.601 по центральному окну кадра (8-битный код после вычитания чёрного). 0 — самый тёмный допустимый кадр, 255 — потолок шкалы. После `imx219_open` режим **авто**, цель **80**.

```
imx219_status_t imx219_set_brightness_mode(imx219_t *cam, imx219_brightness_mode_t mode);
imx219_brightness_mode_t imx219_get_brightness_mode(const imx219_t *cam);

imx219_status_t imx219_set_brightness(imx219_t *cam, uint8_t level);
imx219_status_t imx219_get_brightness(const imx219_t *cam, uint8_t *level);

imx219_status_t imx219_get_brightness_status(const imx219_t *cam,
                                            imx219_brightness_status_t *st);
```

`imx219_brightness_mode_t`:

| Значение | Поведение |
| --- | --- |
| `IMX219_BRIGHTNESS_MANUAL` | `level` задаёт светимость напрямую. Автомат останавливается. Выдержка и оба усиления пересчитываются из `level` и пишутся в сенсор |
| `IMX219_BRIGHTNESS_AUTO` | `level` — цель автомата. Цикл меряет центр кадра и подбирает выдержку и усиление, пока измеренная яркость не попадёт в цель |

Ручной режим не следит за сценой: выставили 80 — связка выдержка/усиление замирает, пока её не сменят. Прямой вызов `imx219_set_exposure_lines`, `imx219_set_analog_gain` или `imx219_set_digital_gain_q88` тоже переводит камеру в ручной режим и оставляет остальные ступени как были.

Автомат:

- мерит центральное окно, не края кадра и не первые строки;
- ждёт не меньше 100 мс и трёх кадров, прежде чем править регистры: банк кадра сенсора применяет выдержку и усиление со следующего кадра;
- крупный шаг ограничен множителем 0.25…4 за один цикл;
- при пересвете уменьшает связку, а не держит цель ценой клиппинга;
- упёршись в 1516 строк, код 232 и цифровое 4×, остаётся на этом потолке. Кадр при этом может быть темнее цели. Это предел света, который матрица может набрать, а не отказ вызова.

`imx219_brightness_status_t` отдаёт: режим, заданный `level`, измеренную яркость 0…255, строки выдержки, код аналогового усиления, цифровое усиление, долю пересвеченных пикселей окна и флаг `at_limit`. Флаг `at_limit` равен истине, когда все три ступени на максимуме, а измеренная яркость всё ещё ниже цели.

`level` вне смысла шкалы не бывает: аргумент — байт 0…255. Неоткрытая камера даёт `IMX219_ERR_STATE`.

### 7.5 Таблицы калибровки цвета

Таблица применяется после демозаики и вычитания чёрного, в линейном свете: сначала баланс белого, затем матрица 3×3, затем один раз передаточная функция sRGB. Демозаика эту таблицу не заменяет.

```
imx219_status_t imx219_set_color_table(imx219_t *cam, imx219_color_table_t id);
imx219_status_t imx219_get_color_table(const imx219_t *cam, imx219_color_table_t *id);
imx219_status_t imx219_get_color_table_info(imx219_color_table_t id,
                                           imx219_color_table_info_t *info);
```

`imx219_color_table_info_t` содержит имя, цветовую температуру в кельвинах, четыре коэффициента баланса (R, Gr, Gb, B) и матрицу 3×3. Коэффициенты — в линейном Q8.8, `0x0100` = 1.000. Поле `measured` истинно только у таблицы, снятой с этой камеры (этот объектив и этот IR-cut).

Каталог для IMX219 на этой плате:

| ID | Источник света | Температура | Состояние |
| --- | --- | --- | --- |
| `IMX219_CC_IDENTITY` | нет, нейтральные коэффициенты | — | В прошивке сейчас. Баланс R=Gr=Gb=B=`0x0100`, матрица единичная. Это не измеренная калибровка сенсора |
| `IMX219_CC_HALOGEN_3200` | галоген, условие измерения даташита (CM500S, 3200 K) | 3200 K | Слот этой камеры |
| `IMX219_CC_TL84` | флуоресцентная лампа TL84 / F11 | 4000 K | Слот этой камеры |
| `IMX219_CC_D50` | горизонт, вспышка, D50 | 5003 K | Слот этой камеры |
| `IMX219_CC_D65` | дневной свет D65 | 6504 K | Слот этой камеры |
| `IMX219_CC_D75` | тень, D75 | 7504 K | Слот этой камеры |
| `IMX219_CC_AUTO` | выбор ближайшей измеренной таблицы по центру кадра | оценивается | Режим, не набор коэффициентов |

После `imx219_open` выбрана `IMX219_CC_IDENTITY`.

Неизвестный ID даёт `IMX219_ERR_COLOR_TABLE`, активная таблица не меняется.

`IMX219_CC_AUTO` оценивает цветовую температуру по центральному нейтральному полю и включает ближайшую таблицу с `measured = true`. Пока ни один слот не измерен, AUTO остаётся на `IMX219_CC_IDENTITY`. Статус цвета читается через `imx219_get_color_status`: поле `uncalibrated` и `info->measured` применённой таблицы.

```
imx219_status_t imx219_get_color_status(const imx219_t *cam,
                                       imx219_color_status_t *st);
```

`st` отдаёт запрошенный ID, фактически применённый ID, оценку температуры в кельвинах и `uncalibrated`. Для ручного выбора измеренного слота `uncalibrated` ложен. Для IDENTITY и для AUTO без измерений — истинен.

Числа матрицы в этот файл не записываются: их снимают с этой камеры по цветовой мишени при фиксированных выдержке, усилении и выключенном AUTO яркости. Чужие коэффициенты другого модуля IMX219 сюда не подставляются. Объектив и IR-фильтр этой платы входят в таблицу наравне с кристаллом.

Синтетический тест RAW (файл-эталон, не сенсор) всегда идёт через `IMX219_CC_IDENTITY`. Таблицу сенсора на него не накладывают.

### 7.6 Надпись на видео

На картинку HDMI наносятся две строки. Сенсор, демозаика и `imx219_grab` эту надпись не содержат: цифры рисуются только в кадре, который уходит на экран.

```
imx219_status_t imx219_set_osd(imx219_t *cam, const imx219_osd_t *osd);
imx219_status_t imx219_get_osd(const imx219_t *cam, imx219_osd_t *osd);
imx219_status_t imx219_get_stream_stats(const imx219_t *cam,
                                       imx219_stream_stats_t *st);
```

`imx219_osd_t`:

| Поле | Смысл |
| --- | --- |
| `enable` | `true` — обе строки видны. `false` — экран без надписи, счётчики при этом продолжают копиться |
| `corner` | Угол: `IMX219_OSD_TOP_LEFT`, `IMX219_OSD_TOP_RIGHT`, `IMX219_OSD_BOTTOM_LEFT`, `IMX219_OSD_BOTTOM_RIGHT` |

После `imx219_open` надпись выключена, угол — левый верхний, отступ 16 пикселей от краёв. Неизвестный угол даёт `IMX219_ERR_RANGE`. Неоткрытая камера даёт `IMX219_ERR_STATE`. Смена `enable` и угла попадает на следующий кадр экрана и поток не останавливает.

Пока `enable` истинен, в выбранном углу две строки белым шрифтом с тёмной обводкой:

```
FPS 30.0
DROP 0
```

`FPS` — сколько новых кадров реально сменилось на экране за последнюю секунду. Надпись показывает измеренную частоту показа. Одна цифра после запятой, строка обновляется раз в секунду. Повтор одного и того же кадра на HDMI, когда режим камеры ниже частоты развёртки монитора, в `FPS` не входит.

`DROP` — сколько кадров сенсора за последние **5 минут** (300 с, скользящее окно) не стали новой картинкой на экране. Кадр считается отброшенным, если к приходу следующего кадра сенсора предыдущий так и не был показан: буфер затёрт или дисплей его не забрал. Через 300 с такой кадр из суммы выходит сам. `imx219_start` обнуляет окно. Пока с запуска прошло меньше 5 минут, на экране всё равно написано `DROP`, а в статусе видно, какая часть окна уже набрана.

`imx219_stream_stats_t` отдаёт те же величины, что и надпись, плюс сырьё:

| Поле | Смысл |
| --- | --- |
| `fps_milli` | Кадры экрана за последнюю секунду, в тысячных. `30000` на надписи выглядит как `FPS 30.0` |
| `shown_1s` | Целое число новых кадров экрана за эту секунду |
| `drops_5min` | Отброшенные кадры в окне 300 с. Это число в строке `DROP` |
| `window_ms` | Сколько миллисекунд окна уже заполнено, от 0 до 300000 |
| `mode_fps_milli` | Частота, заданная режимом, в тех же тысячных. На экран не выводится |

---

## 8. Что ещё стоит добавить в API

Ниже функции, которых в минимальном контракте нет, и которые сенсор IMX219 уже умеет. Имеет смысл провести их следующим слоем, не смешивая с разрешением и форматом.

### 8.1 Экспозиция

Электронный затвор в строках: `coarse_integration_time` от 1 до `frame_length_lines − 4`. Дольше этого кадра — режим длинной выдержки: длина кадра растёт вместе с выдержкой, частота падает. `fine_integration_time` только для чтения, в даташите 500.

```
imx219_status_t imx219_set_exposure_lines(imx219_t *cam, uint16_t lines);
imx219_status_t imx219_get_exposure_lines(const imx219_t *cam, uint16_t *lines);
imx219_status_t imx219_set_exposure_us(imx219_t *cam, uint32_t microseconds);
```

Пользовательская яркость — раздел 7.4. Эти три функции — нижний слой ручного режима: прямой записи строками и кодом усиления. На кристалле автоэкспозиции нет, автомат раздела 7.4 живёт в API.

### 8.2 Усиление

Аналоговое и цифровое задаются независимо, только глобально (не по цвету).

Аналоговое: код `X` от 0 до 232, кратность `256 / (256 − X)`. Код 0 = 1×, код 232 ≈ 10.7×.

Цифровое: беззнаковое 8.8, `0x0100` = 1.0×. Минимум, максимум и шаг сенсор отдаёт своими регистрами capability; в прошивке программный потолок сейчас 4× (`0x0400`).

```
imx219_status_t imx219_set_analog_gain(imx219_t *cam, uint8_t code);
imx219_status_t imx219_set_digital_gain_q88(imx219_t *cam, uint16_t gain_q88);
```

Смена только усиления и выдержки не вставляет пустой кадр. Смена биннинга, вертикального прореживания и вертикальной ориентации может удлинить гашение на время выдержки.

### 8.3 Ориентация

```
imx219_status_t imx219_set_orientation(imx219_t *cam, imx219_orient_t orient);
```

`orient` — 0…3, как в таблице раздела 6. Функция сама ставит согласованную фазу демозаики. Отдельный `imx219_set_demosaic` после неё нужен только если фазу хотят переопределить.

### 8.4 Окно и биннинг

Активная область 3280×2464. Ниже неё — вырезка. Полный угол обзора при меньшем кадре — биннинг, а не цифровой масштаб.

| Биннинг | Горизонталь | Вертикаль | Смысл |
| --- | --- | --- | --- |
| нет | ×1 | ×1 | полный пиксель, 1080p именно так |
| ×2 | аналоговый | ×2 | меньше шума, выше кадровая частота, полный угол |
| ×4 | цифровой | ×4 | ещё мельче |
| ×2 analog special | аналоговый особый | ×2 | режим даташита для высокой частоты около 720p |

Сложение или среднее задаётся отдельно по горизонтали и вертикали.

```
imx219_status_t imx219_set_crop(imx219_t *cam,
                               uint16_t x, uint16_t y,
                               uint16_t w, uint16_t h);
imx219_status_t imx219_set_binning(imx219_t *cam,
                                  imx219_binning_t bin,
                                  imx219_bin_cal_t cal);
```

`x` и `y` чётные, чтобы не сдвинуть фазу Байера. Окно не выходит за 3280×2464.

### 8.5 Тестовый рисунок

Генератор на кристалле, без света. Режимы даташита: выключен, заливка, цветные полосы 100 %, полосы с уходом в серый, PN9, 16 сегментов, 16 сегментов инверсия, счётчик столбцов, счётчик столбцов инверсия, PN31. Для заливки задаются R, Gr, B, Gb.

```
imx219_status_t imx219_set_test_pattern(imx219_t *cam,
                                       imx219_tp_t pattern,
                                       const imx219_tp_color_t *solid);
```

Удобно отделять неисправность оптики от неисправности CSI-2 и демозаики.

### 8.6 Температура

Датчик перехода кристалла, цель −10…+95 °C, допуск даташита ±5 °C. Включается отдельным битом, результат — в регистре статуса.

```
imx219_status_t imx219_read_temperature_c(imx219_t *cam, int16_t *celsius);
```

Имеет смысл для длинной выдержки и для ограничения аналогового усиления при нагреве.

### 8.7 Два банка кадра

Регистры кадра лежат в банке A и банке B и защёлкиваются по вертикальной синхронизации. Банк позволяет приготовить следующую экспозицию и усиление, не трогая текущий кадр.

```
imx219_status_t imx219_select_frame_bank(imx219_t *cam, imx219_bank_t bank);
```

### 8.8 Встроенные строки

Сенсор может отдать две строки embedded data сразу после Frame Start: индекс регистра и значение. По ним снаружи проверяют, что в этот кадр попали именно записанные выдержка, усиление и формат.

```
imx219_status_t imx219_set_embedded_data(imx219_t *cam, bool enable);
```

### 8.9 Чёрный и потолок кода

Чёрный уровень фиксирован: 64 для RAW10 и COMP8, 16 для RAW8. Потолок 10-битного кода — 1023. API лучше отдавать это чтением, а не настройкой.

```
imx219_status_t imx219_get_levels(const imx219_t *cam,
                                 uint16_t *black,
                                 uint16_t *white);
```

Выбор баланса белого и матрицы 3×3 — раздел 7.5. Чёрный уровень из этой функции туда входит как точка отсчёта таблицы.

### 8.10 Идентификация и сброс

```
imx219_status_t imx219_read_chip_id(imx219_t *cam, uint16_t *id);
imx219_status_t imx219_reset(imx219_t *cam);
```

`id` должен быть `0x0219`. `imx219_reset` повторяет последовательность XCLR и возвращает конфигурацию по умолчанию из раздела 7.1, не оставляя сенсор в случайном банке.

---

## 9. Коды возврата

| Код | Смысл |
| --- | --- |
| `IMX219_OK` | Вызов применён |
| `IMX219_ERR_PROBE` | Нет ответа или идентификатор не `0x0219` |
| `IMX219_ERR_STATE` | Поток уже идёт или ещё не открыт |
| `IMX219_ERR_MODE` | Нет такого разрешения |
| `IMX219_ERR_FPS` | Частота не из стандартного набора этого разрешения |
| `IMX219_ERR_PIXFMT` | Формат не RAW10, RAW8 или COMP8 |
| `IMX219_ERR_LANES_FIXED` | Запрошено не 2 линии. 4 линии на этой модели недоступны |
| `IMX219_ERR_BANDWIDTH` | Поток не влезает в 2×912 Мбит/с |
| `IMX219_ERR_MOSAIC` | Фаза 4…11 запрошена без поддержки в демозаике |
| `IMX219_ERR_RANGE` | Выдержка, усиление или окно вне пределов сенсора |
| `IMX219_ERR_COLOR_TABLE` | Нет такой таблицы калибровки |
| `IMX219_ERR_NOSPC` | Буфер кадра короче кадра |
| `IMX219_ERR_TIMEOUT` | Кадр не пришёл за отведённое время |
| `IMX219_ERR_I2C` | Ошибка записи или чтения CCI. Чтение — с Repeated Start |

---

## 10. Порядок типичного вызова

1. `imx219_open`
2. `imx219_set_pixel_format` — RAW10, RAW8 или COMP8
3. `imx219_check_link` — разрешение, частота, формат
4. `imx219_set_video_mode` — одно из разрешений раздела 3 и частота раздела 4
5. `imx219_set_demosaic` — фаза 0…3, по умолчанию GBRG
6. `imx219_set_brightness_mode` / `imx219_set_brightness` — авто с целью или ручной уровень 0…255
7. `imx219_set_color_table` — таблица источника света или `IMX219_CC_AUTO`
8. `imx219_set_lanes(IMX219_LANES_2)` — подтверждение, значение не меняется
9. `imx219_set_osd` — включить надпись `FPS` и `DROP` в выбранном углу
10. `imx219_start`
11. `imx219_grab` в цикле
12. `imx219_stop`, `imx219_close`

Шаги 6, 7 и 8 можно не вызывать: после `imx219_open` уже стоят автояркость с целью 80, таблица `IMX219_CC_IDENTITY` и 2 линии. Шаг 9 нужен, чтобы цифры появились на HDMI: после открытия надпись выключена. Те же числа без рисунка на кадре читаются через `imx219_get_stream_stats`.
